---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: litellm

resources:
  - ingressroute.yaml          # LAN-only: litellm.example.com (UI + full API)
  - ingressroute-public.yaml   # Public via CF tunnel: litellm-ext.example.com (/v1 only)
