---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: traefik

resources:
  # Pre-saved certificate secret (SealedSecret) to avoid Let's Encrypt rate
  # limits during testing and development.
  # - example-com-wildcard-tls-sealed.yaml
  # PRODUCTION
  - certificate-prod.yaml
  # STAGING
  # - certificate-staging.yaml
