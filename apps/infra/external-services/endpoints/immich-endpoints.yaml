---
apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: immich
  namespace: traefik
  labels:
    kubernetes.io/service-name: immich
addressType: IPv4
endpoints:
  - addresses:
      - 10.30.0.30
    conditions:
      ready: true
ports:
  - port: 8081
    name: http
    protocol: TCP
