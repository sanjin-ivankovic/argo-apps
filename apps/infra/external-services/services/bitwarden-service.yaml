---
apiVersion: v1
kind: Service
metadata:
  name: bitwarden
  namespace: traefik
spec:
  clusterIP: None
  ports:
    - port: 8080
      targetPort: 8080
      name: http
      protocol: TCP
---
apiVersion: v1
kind: Service
metadata:
  name: bitwarden-smtp
  namespace: traefik
spec:
  clusterIP: None
  ports:
    - port: 5000
      targetPort: 5000
      name: http
      protocol: TCP
