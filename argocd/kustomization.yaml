---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Single ApplicationSet that generates every Application (infra, data,
  # services, user) from the per-app config.yaml under apps/<tier>/<name>/.
  # This replaces the former hand-written infrastructure Application list.
  - apps-set.yaml
