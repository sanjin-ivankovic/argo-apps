---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Infrastructure applications (platform components)
  - infrastructure

  # User applications (deployed via ApplicationSet)
  - applications
